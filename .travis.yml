language: python
python:
  - "2.7"

install:
  - pip install pep8
  - pip install conda
  - conda create -y -n newenv numba
  - source activate newenv
  - pip install futures
  - pip install codecov

script:
  - pep8 .
  - coverage run test.py

after_success:
  - codecov

